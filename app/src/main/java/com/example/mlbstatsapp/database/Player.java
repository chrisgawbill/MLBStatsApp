package com.example.mlbstatsapp.database;


//Empty interface was added to allow for the creation of a Player list to be inserted
//with all types of baseball Players whose stats aren't read the same.
public interface Player {
}
